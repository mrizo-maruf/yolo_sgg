# YOLO-SGG — Scene Graph Generation Requirements
# Python 3.10+ recommended

# ── Core ──────────────────────────────────────────────
numpy>=1.24.0
opencv-python>=4.8.0
torch>=2.0.0
torchvision>=0.15.0
Pillow>=10.0.0

# ── YOLO / Object Detection ──────────────────────────
ultralytics>=8.1.0

# ── 3D / Geometry ────────────────────────────────────
open3d>=0.17.0
shapely>=2.0.0
trimesh>=3.21.0
pyvista>=0.40.0

# ── Scene Graph & Graphs ─────────────────────────────
networkx>=3.1

# ── Visualization ────────────────────────────────────
matplotlib>=3.7.0
rerun-sdk>=0.16.0

# ── Configuration & Utilities ────────────────────────
omegaconf>=2.3.0
tqdm>=4.65.0
PyYAML>=6.0

# ── Symbolic Math (relationship calculations) ────────
sympy>=1.12

tensorboard>=2.12.0
# ── Optional: pytorch3d (used in kg_nav_run.py) ─────
# Requires a build from source or a matching prebuilt wheel:
#   pip install "git+https://github.com/facebookresearch/pytorch3d.git"
# pytorch3d

# ── Optional: VL-SAT model dependencies ─────────────
# Needed only when running the vl_sat_model/ module
# torch-geometric>=2.3.0
# torch-scatter>=2.1.0
# ftfy>=6.1.0
# regex>=2023.0
# plyfile>=1.0.0
# clip  (install via: pip install git+https://github.com/openai/CLIP.git)

# ── Development & Testing (optional) ────────────────
# pytest>=7.3.0
# jupyter>=1.0.0
